import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// Placeholder Vite config. Dev-server proxy and build options will be
// added once API integration begins.
export default defineConfig({
  plugins: [react()],
})
